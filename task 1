int limit(int m,int n)
{
    if (m == n) return m;
    return m+limit(m+1,n);
}
int m = Convert.ToInt32(Console.ReadLine());
int n = Convert.ToInt32(Console.ReadLine());

Console.WriteLine($"{limit(m,n)}");
